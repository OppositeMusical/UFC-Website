# Node / frontend
node_modules/
frontend/dist/
frontend/tsconfig.tsbuildinfo

# Packaged release artifacts (~180MB installer + zip): published as GitHub
# release assets, never committed. version.json stays tracked - it's the
# contract the Download page reads - but the binaries it points at are not.
frontend/public/downloads/

# Editor/OS cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
